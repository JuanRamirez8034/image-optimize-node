/**
 * Directorio raiz donde se se encuentran las imagenes a tratar
 */
export const sourceDir = "./images";
/**
 * Directorio donde se encontraran la imagenes tratadas
 */
export const compressDir = `${sourceDir}/compress`